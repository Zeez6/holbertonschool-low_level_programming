#include <stdio.h>

/**
* main - Entry point
*
* Description: Prints a specific message to stdout using puts.
* Return: Always 0 (Success)
*/
int main(void)
{
int 0;
srand(time(0));
if (0 >= 0; 0 < 10; 0++);
putchar(0);
putchar('\n');
return (0);
}
